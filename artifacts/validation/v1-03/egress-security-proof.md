# V1-03 egress foundation

The WooCommerce boundary rejects non-HTTPS, userinfo and non-public IPv4/IPv6 destinations, pins the validated DNS address for HTTPS, disables redirects, limits response size and requires JSON.
